import 'package:flutter/material.dart';
import 'package:{{ snakeCase appName }}/app/core/theme/ui_config.dart';
import 'package:{{ snakeCase appName }}/features/home/views/widgets/search_bar.dart';

class WishlistHeader extends StatelessWidget {
  final Function(String) onSearch;

  const WishlistHeader({super.key, required this.onSearch});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const SizedBox(height: UIConfig.paddingSmall),
        CustomSearchBar(onSearch: onSearch),
        const SizedBox(height: UIConfig.paddingXLarge),
      ],
    );
  }
}
